<!-- Main News Slider Start -->
<div class="container-fluid">
    <div class="row">
        <!--<div class="col-lg-12 px-0">-->
        <div class="col-lg-7 px-0">
            <div class="owl-carousel main-carousel position-relative">

                <% @slider_articles.each do |slider_article| %>
                    <div class="position-relative overflow-hidden" style="height: 500px; border: solid 5px; border-color: white;">
                        <% if !slider_article.main_image_attachment.nil? %>
                            <%= image_tag slider_article.main_image, class: "img-fluid h-100", style: "object-fit: cover;" %>
                        <% end %>
                        <div class="overlay">
                            <div class="mb-2">
                                    <% slider_article.categories.each do |category| %>
                                        <%= link_to category.name, category_path(category), class: "badge badge-primary text-uppercase font-weight-semi-bold p-1 mr-1", data: { turbo: false } %>
                                    <% end %>
                                <br>    
                                <a class="text-white" href="#">
                                    <%= slider_article.created_at.strftime("%d/%m/%Y") %>
                                </a>
                            </div>
                                <%= link_to slider_article.title, article_path(slider_article), class: "h2 m-0 text-white text-uppercase font-weight-bold", data: { turbo: false } %>
                        </div>
                    </div>
                <% end %>
            </div>
        </div>
        
        <div class="col-lg-5 px-0">
            <div class="row mx-0">
                <% @articles_for_top.each do |subarticle| %>
                     <div class="col-md-6 px-0">
                         <div class="position-relative overflow-hidden" style="height: 250px; border: solid 5px; border-color: white;">
                            <% if !subarticle.main_image_attachment.nil? %>
                                <%= image_tag subarticle.main_image, class: "img-fluid w-100 h-100", style: "object-fit: cover;" %>
                            <% end %>
                             <div class="overlay">
                                 <div class="mb-2">
                                    <% subarticle.categories.each do |category| %>
                                        <%= link_to category.name, category_path(category), class: "badge badge-primary text-uppercase font-weight-semi-bold p-1 mr-1", data: { turbo: false } %>
                                    <% end %>
                                     
                                     <br>
                                     <a class="text-white" href=""><small><%= subarticle.created_at.strftime("%d/%m/%Y") %></small></a>
                                 </div>
                                 <%= link_to subarticle.title, article_path(subarticle), class: "h6 m-0 text-white text-uppercase font-weight-semi-bold", data: { turbo: false } %>
                             </div>
                         </div>
                     </div>   
                <% end %>
            </div>
        </div>
    </div>
</div>

<!-- Main News Slider End -->